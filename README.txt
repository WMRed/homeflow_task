Important note:

This website was tested using Google Chrome, but it will only read from the URLs if you install 
the "Allow-Control-Allow-Origin" plug-in from the Chrome Web Store, switch it on and refresh the page.

